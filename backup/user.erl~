-module(user).
-behaviour(application).

-export([
    start/0, start/2, stop/1, get_ip_db_file/0
]).

start() ->
    application:start(user).

start(_StartType, _StartArgs) ->
    lager:info("Starting user application..."),
    user_sup:start_link().

stop(_State) ->
    ok.

